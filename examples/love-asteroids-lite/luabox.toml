[package]
name = "asteroids-lite"
version = "0.1.0"
edition = "5.1"       # LÖVE runs LuaJIT (5.1-compatible)

[build]
mode = "love"         # `luabox bundle` packages a .love archive
out = "dist"

[types]
strict = true
defs = ["love2d"]     # ambient LÖVE API types from defs/love2d.d.lua

[dependencies]
