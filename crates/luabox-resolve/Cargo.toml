[package]
name = "luabox-resolve"
description = "luabox: luabox.toml manifest model, validation and round-tripping (Distribution context)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
serde.workspace = true
toml_edit.workspace = true
# The dialect *compatibility* model ([`dialect`]) classifies
# `luabox_syntax::Dialect` values as families; it never parses Lua (SPEC.md
# §16 — Distribution "never parses syntax").
luabox-syntax.workspace = true

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
