---
column: doing
labels: [wave]
priority: high
agent: opus-13a
live: true
status: launching
updatedAt: 2026-07-29T19:52:00.000Z
---
# Wave 13a — goto/label/break legality diagnostics (#44)

Owner pre-release burn-down. Unresolved goto, duplicate label, break outside a loop must diagnose like reference Lua rejects them. HIR already resolves goto targets (crates/luabox-hir/src/hir.rs:185).

## Comments

- **opus-13a** (2026-07-29T19:52:00.000Z): Shipped on `sprint/w13a-goto-legality`. **The pass** is `crates/luabox-hir/src/validate.rs` (new, 380 production lines + a 20-test module) — `control_flow(file, &lowered, dialect) -> Vec<Diagnostic>`, a walk over the HIR that *reads* the goto→label resolution `lower` already performs (`crates/luabox-hir/src/lower.rs:743` `resolve_gotos`) rather than re-deriving it, and adds the legality judgement the TODO at `crates/luabox-hir/src/hir.rs:185` deferred (comment rewritten, `crates/luabox-hir/src/hir.rs:185-188`). `luabox-hir` gains a `luabox-diag` dep (`crates/luabox-hir/Cargo.toml:12-18`) so the one pass emits `luabox_diag::Diagnostic` directly instead of a `DialectError`-style struct each frontend re-maps — sibling `luabox-types` already does exactly that, and `luabox-diag` is a leaf (serde only), so §16 stays acyclic. Precise spans needed one new side table: `goto_names: HashMap<HirId, TextRange>` for the goto's *name token* (`crates/luabox-hir/src/lower.rs:64,284,729`, `crates/luabox-hir/src/file.rs:26,95-102`) — ranges stay out of HIR nodes. **Codes** `LB0020` no visible label / `LB0021` label already defined / `LB0022` break outside a loop, a new LB002x "edition-independent source legality" decade beside the LB001x dialect block, registered with full explain pages at `crates/luabox-diag/src/registry.rs:61-80,449-572`; the registry's `every_registered_code_is_reachable_by_its_rendered_form` covers them by construction. **Wiring** — `check`: `crates/luabox-cli/src/check_cmd.rs:303-321` (pass 3 of now-four; lowers the file itself because `luabox_types::FileArtifacts` keeps its HIR private and that crate was off-limits this wave — one extra `lower()` per clean-parse file); `lint`: `crates/luabox-lint/src/lib.rs:114-124`, free off the engine's own lowering, emitted like parse errors — no tier, not suppressible by `---@luabox-ignore`; `lsp`: `crates/luabox-lsp/src/diagnostics.rs:117-136`, which rides in with the lint engine and re-tags non-`LB05xx` codes to the `luabox` source so the editor shows a hard error, not a rule finding. All three skip the pass when the parse is dirty — block structure recovered around a missing `end` is a guess. **Rules are differential, not guessed**: derived from `lparser.c` (`undefgoto`, `checkrepeated`, `movegotosout`) and verified cell-for-cell against `luac5.4 -p` / `luac5.1 -p` on a 53-program matrix (43 × 5.4, 10 × 5.1 break-only). Two findings the matrix caught: (1) 5.4 rejects a *nested* label shadowing an outer one (`::a:: do ::a:: end`) where 5.2/5.3/LuaJIT accept it — `repeated_label_scope` is dialect-aware so luabox never rejects what your edition's compiler accepts (`crates/luabox-hir/src/validate.rs:60-105` `repeated_label_scope`); (2) my first cut pre-collected each block's labels and false-positived on `do ::a:: end ::a::`, which luac accepts — fixed by splitting `Scopes` into `visible` (whole-block, for forward gotos) and `declared` (positional, for `checkrepeated`) at `crates/luabox-hir/src/validate.rs:147-150`. One reference rule is deliberately **not** implemented — a forward `goto` jumping into a local's scope — because `;` is erased on lowering so the HIR cannot express "void statement" and the check would re-implement the parser rather than read the resolution; under-approximation only, pinned by a test (`a_goto_jumping_into_a_local_scope_is_knowingly_accepted`) and written up in `docs/03-reference/02-limitations.md:91-111` (the "not diagnosed" entry is gone; the section is now "shipped — #44 closed; one rule left out"). **Tests**: 20 new unit tests in `crates/luabox-hir/src/validate.rs` (61 lib tests, was 41); new `crates/luabox-cli/tests/features/frontend/control-flow.feature` (53 scenarios from 10 keywords: 19 illegal programs × edition, 24 legal counterparts, 3 explain pages, plus did-you-mean present/absent, span shape, duplicate's secondary label, lint parity, ignore-does-not-silence and dirty-parse restraint) plus a `no control-flow diagnostic is reported` step at `crates/luabox-cli/tests/acceptance.rs:358-373`, and 2 LSP scenarios at `crates/luabox-cli/tests/features/lsp/diagnostics.feature:121-147`. **Verification**: `cargo test --workspace` 2256 passed / 0 failed (was 2236); acceptance 793 scenarios / 4255 steps (was 740/3987, 35 features); lsp_acceptance 189/1374 (was 187/1359); clippy `--workspace --all-targets -D warnings` and `cargo fmt --all --check` clean; unit coverage luabox-hir **98.77%**, luabox-lint **99.42%**, luabox-lsp **97.82%**, luabox-diag **97.59%**, luabox-cli **93.79%** (floor 92) with `validate.rs` 96.85% and `check_cmd.rs` 98.99%; `bash scripts/examples.sh` on a release build **ALL GREEN** (7 examples, incl. the timemachine bundle executing on lua5.1) and all 19 `corpus/**.lua` files scan clean — no example or corpus fixture carried an illegal goto/break. CHANGELOG Unreleased → Fixed; README's `check` row now names the new legality. Not touched: `crates/luabox-types` (concurrent wave).
