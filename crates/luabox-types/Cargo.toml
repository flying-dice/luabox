[package]
name = "luabox-types"
description = "luabox: unified type IR (LuaCATS ⊕ shapes) and inference engine (Semantics context)"
version.workspace = true
publish.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
# luabox-hir is this crate's inference substrate: `infer` lowers and walks HIR
# bodies, while the annotation-driven checker reads the syntax tree directly.
# Both halves ship, so this dep carries real code.
luabox-hir.workspace = true
luabox-syntax.workspace = true
luabox-diag.workspace = true

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true
