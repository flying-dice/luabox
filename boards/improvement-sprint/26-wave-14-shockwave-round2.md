---
column: done
labels: [wave, review-round]
priority: high
agent: opus-w14
live: true
status: Done — PR #47 APPROVED (round 11) and merged as a63842d; issues #44/#33/#38/#39/#30 closed with evidence
updatedAt: 2026-08-01T15:05:00.000Z
---
# Wave 14 — Shockwave round-2 threads on PR #47

1 (bug): --target must reach the control-flow legality pass (crates/luabox-cli/src/check_cmd.rs pass 3) + target scenarios in control-flow.feature. 3 (question->fix): keep luals parity AND the runtime truth - new lint rule for setmetatable(t, C) where C.__index is never assigned; answer the thread with the decision. 4: replace the /100==5 magic decade with a luabox-diag band query + registry-asserted invariant. 5: measure LSP initialize->first-diagnostics on a big annotated rock tree; parallelize the LSP harvest (rayon) or move it off the critical path; ship the number. Path-filter issue already fixed directly (3f3ada0).

## Comments

- **opus-w14** (2026-08-01T09:40:00.000Z): All four round-2 threads plus round-3's A/B/C and the precision addendum are fixed on sprint/w14-shockwave-round2 (8 commits, 03febcc..46388f6). **1** — pass 3 now runs for the same dialect pair as pass 2 through one shared `dialect_passes` helper, deduplicated by (code, span), one lowering feeding both (crates/luabox-cli/src/check_cmd.rs:305-360, :389). Repro flips: edition 5.2 + `::a:: do ::a:: end` + `--target 5.4` was 0 errors/exit 0, now LB0021/exit 1. Found and fixed a second call site the brief asked me to check: `luabox build --target 5.4` on the same 5.2 source emitted a tree `luac5.4 -p` refuses to load and exited 0 — the check gate stays edition-only by design, so the catch is in the residual validation of the lowered output (crates/luabox-cli/src/build_cmd.rs:344-372). `lint`/`lsp` have no `--target` flag (verified via `--help`) — out of scope. Differential runner grew a target column (edition pinned to 5.2), 56 -> 112 cells, all agree. **3** — new `metatable-without-index` lint, LB0510, suspicious tier (crates/luabox-lint/src/rules/metatable_without_index.rs, registry+explain at crates/luabox-diag/src/registry.rs:206-210). Checker output is unchanged (parity tests green); the Counter repro now warns while `luabox check` still exits 0 and `lua5.4` still raises `attempt to call a nil value (method 'value')`. **Parity status: luabox-specific** — luals ships no equivalent diagnostic, so `[lint] metatable-without-index = "allow"` restores exact luals behaviour; stated in the explain page and in docs/03-reference/02-limitations.md:87-126, which now says plainly that member RESOLUTION falls through (not just tag propagation) and scopes the "false negatives, never false positives" line to unresolved receivers. **4** — `Code::is_lint` (band) + `Code::is_lint_rule` (band minus LB0500, which is the crate's own suppression-syntax code, not a rule) at crates/luabox-diag/src/code.rs:54-85; used at crates/luabox-lsp/src/diagnostics.rs:154. Both unwritten invariants asserted lint-side where the registry lives: every rule code is a rule code, and only rules carry fixes (crates/luabox-lint/src/tests.rs:940-1000). Grep is clean of magic-band arithmetic; `Code::block()` is documented as unusable for this and pinned by a test. **5** — LSP harvest parallelized on the workspace rayon pool via `harvest_file` + `RockSurfaces::fold` (crates/luabox-lsp/src/server.rs:390-425); pool is the global one `real_main` pins to 16 MiB (crates/luabox-cli/src/main.rs:263-272). **First LSP number this repo has ever had**: 50 files / ~103 kLOC annotated rock tree, real stdio `initialize`->first `publishDiagnostics`, 4 cores, 7 runs — sequential median 2270 ms, same binary at RAYON_NUM_THREADS=1 median 2095 ms, parallel median 545 ms (min 529). ~3.8x; 8 ms with the tree removed, so the harvest was the whole wait. **Deviation, flagged for Shockwave**: 545 ms is AT the 500 ms threshold, not comfortably under, and I did NOT move the harvest off the critical path. An asynchronous republish buys ~0.5 s at the cost of a window where rock-typed code is diagnosed against an empty rock layer — LB0305/LB0306 flashing red and clearing. Judgment recorded at the call site; one line overrules it. **A** — `collect_rock_sources` sorted `Vec<PathBuf>`, whose component-wise Ord put `pl/init.lua` before `pl.lua`; now sorts raw bytes (crates/luabox-manifest/src/layout.rs:453-470), which reproduces `resolve_candidates` order on every platform ('.'=0x2E < '/'=0x2F < '\\'=0x5C). Measured before: on their fixture `check` reported LB0300 and the LSP reported nothing; after, both report LB0300. Pinned by a `collect_rock_sources` test over a tree that actually holds the colliding pair (the old first-wins test hand-built its vec), plus the fixture asserted through both frontends. rocks.rs:80-95 and decisions/09 reworded. **B** — `carrier_var_classes` (crates/luabox-types/src/env.rs:1637-1700) filled one map with both spellings using `insert`, so a later `---@class` whose NAME equalled an earlier class's carrier VARIABLE clobbered the lexical binding. Lexical now beats nominal in ordered passes, first-wins within each rank. Both orders of their repro now attach `speak` to `Wrapper` and match what the project-source path does with the identical body (measured all three ways). **C** — validate.rs's claim that 5.1 `goto` is "already reported as LB0010" was wrong: measured LB0001 x2 (goto is not in the 5.1 grammar), so the `check_goto` guard is unreachable from any front-end path while `check_label`'s IS reachable and load-bearing (`::a::` parses under 5.1). Both documented per real reachability (crates/luabox-hir/src/validate.rs:92-135, :203-260); the goto guard KEPT as defence in depth because `control_flow` is `pub` over an already-lowered file, with a test pinning the asymmetry. **Gates**: `cargo test --workspace` 2369 passed / 0 failed; acceptance 841 scenarios / 4543 steps; lsp_acceptance 194 scenarios / 1406 steps; clippy `-D warnings` exit 0; `cargo fmt --all --check` exit 0; control-flow differential 112/112. **Coverage**: unit aggregate 95.55% (floor 95), per-crate all >= 92 — bundle 96.59, cli 93.12, db 99.46, diag 97.09, hir 98.63, lint 99.05, lower 97.93, lsp 97.42, manifest 96.86, syntax 97.55, types 96.90; e2e 85.19% (floor 83). CHANGELOG has Unreleased entries for 1, 3, A and B (user-facing) and an Internal section for 4, 5 and C.
