[package]
name = "geometry"
version = "0.1.0"
edition = "5.4"

[build]
target = "5.4"
out = "dist"

[types]
strict = true
# Ambient LuaCATS definitions: resolves defs/geometry.d.lua by its file stem
# (the same mechanism love-asteroids-lite uses for defs/love2d.d.lua). No
# `.luab` shape modules here — see ../README.md for why, and what that swap
# costs today.
defs = ["geometry"]

[dependencies]
