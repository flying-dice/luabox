//! `luabox bench` — under construction (#26).

use std::path::Path;

use anyhow::bail;

pub fn run(_cwd: &Path) -> anyhow::Result<()> {
    bail!("`luabox bench` is not implemented yet — planned for P4 (see SPEC.md §18)")
}
