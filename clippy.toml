# Proper nouns clippy::doc_markdown should not demand backticks for.
# ".." keeps clippy's built-in default list.
doc-valid-idents = ["LuaJIT", "LuaLS", "LuaCATS", "LuaRocks", "PubGrub", "OpenResty", ".."]
