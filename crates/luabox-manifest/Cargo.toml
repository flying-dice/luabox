[package]
name = "luabox-manifest"
description = "luabox: luabox.toml manifest model, validation and project layout (Distribution context)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
toml_edit.workspace = true

[dev-dependencies]
tempfile.workspace = true
# The schema/parser parity suite (`src/schema.rs`): `serde_json` reads the
# embedded schema and the TOML→JSON projection of every corpus manifest,
# `jsonschema` compiles the schema and validates that corpus against it.
serde_json.workspace = true
jsonschema.workspace = true

[lints]
workspace = true
