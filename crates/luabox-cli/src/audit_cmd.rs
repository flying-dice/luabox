//! `luabox audit` — under construction (#30).

use std::path::Path;

use anyhow::bail;

pub fn run(_cwd: &Path) -> anyhow::Result<()> {
    bail!("`luabox audit` is not implemented yet — planned for P5 (see SPEC.md §18)")
}
