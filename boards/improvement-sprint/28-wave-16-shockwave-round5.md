---
column: doing
labels: [release, review]
priority: high
agent: opus-w16
live: true
status: All 10 round-5 threads fixed on sprint/w16-shockwave-round5; awaiting round 6
updatedAt: 2026-08-01T06:45:00.000Z
---
# Wave 16: Shockwave round-5 threads on PR #47

Round 5 @ 4659cb5: 10 of 15 round-4 threads CLOSED — the whole target-legality axis closed on Shockwave's 324-row enumeration. 10 threads open: the blocking LB0510 regression plus accuracy items. refs #47

Priority by blast radius: **LB0510 regression** (wave 15's false-positive fixes over-suppressed — 20 false negatives, 14 of them new, on a 28-shape matrix with runtime ground truth; release-blocking), then P (LIMITATIONS overstates), M (false "unreachable" claim), N/O/Q (LSP progress + retracted bench figure), R/S (help text + module doc), and the non-blocking bundle K/E/thread-5-follow-up/scenarios.

## Scope

- **LB0510**: regression A (operator suppression ignores instance methods) + regression B (alias arm requires no write); closes the reopened infer.rs:888 thread
- P: rewrite the LIMITATIONS LB0510 subsection to match the fixed code exactly; disclose `__gc`/`__name`
- M: rewrite the residual-arm comment — name the lowering-introduced carve-out as its primary service and the three gate-blind paths honestly
- R: `check --help`'s `--target` wording + SPEC §5 line on which passes the manifest target drives
- S: build_cmd module doc — the gate's control-flow half, and the bundle-path residual twin
- N: work-done progress token around the startup harvest
- O: gate all progress on `window.workDoneProgress`; `pin_worker_stacks` testability
- Q: bench script's retracted 3.84x; record the host next to the committed numbers
- Bundle: K preflight widening, E global source ordering, `Code::is_lint` for the code-action source, four scenario groups

## Comments

- **opus-w16** (2026-08-01T06:45:00.000Z): All 10 round-5 threads fixed on `sprint/w16-shockwave-round5` (3 commits, 2a16efd..7a68c82 plus the wrap-up). **LB0510** — reproduced Shockwave's finding against a real `lua5.4` (5.4.6 on this box, not a built harness: `/usr/bin/lua5.4` and `/usr/bin/lua5.1` are present) over a 32-shape matrix driven end-to-end through `luabox lint` on real projects. BEFORE: 19 shapes crash at runtime, the rule fired on **5** (`D_counter_repro`, `D_plain_fields_only`, `D_rawset_plain_key`, both `E_*` inheritance shapes) — matching Shockwave's count exactly. AFTER: **17** fire, **0** false positives across the 13 shapes that run clean, and the only two crashing shapes still silent are the documented conservative bounds (alias write in a dead branch, alias write in a never-called function), both pinned as tests alongside their *direct*-write twins so the bound is consistent rather than accidental. *Regression A*: `key_kind` classifies any `__`-prefixed key as a metafield and the bail ORed `is_operator_table` in unconditionally, so one `function Counter.__tostring(c)` line silenced a class with colon methods; the Vector2 tutorial class (dot constructor + `:length()` + `__tostring` + `__add`) crashes on `v:length()` and was silent. Fixed with Shockwave's discriminator: another metafield suppresses only when the carrier carries **no instance methods**, where an instance method is a colon-declared `function C:m()` — `self` is implicit, so it is written to be reached as `instance:m()`, which is the lookup a missing `__index` breaks. `function C:__call()` is a metafield spelled with a colon, not a method, so only ordinary keys record one. **Documented choice**: `---@field` members do NOT count (the canonical carrier declares `---@field n integer` for a *data* field) and neither do dot-assigned function fields (`C.new` is called as `C.new()`, never through the metatable); both omissions are false-negative-shaped and are stated as such in the rule doc and in LIMITATIONS. *Regression B*: settling on `Expr::Name` in a `Stmt::Local` meant a bare `local mt = Counter` disabled the rule with no write anywhere. Carrier identity now propagates along local aliases (`Aliases`, chains followed to a root with a bounded walk) and the same `KeyKind` logic applies to the root: literal `__index` or an unreadable key settles, `mt.n = 1` does not, a literal metafield through an alias classifies the carrier subject to the same no-instance-methods discriminator. Reassignment is deliberately *not* alias-resolved — `mt = {}` rebinds a name, it does not touch the carrier's table — and a test pins that. The `setmetatable` site also resolves through the alias root, so a carrier that is itself an alias (`---@class Counter local Counter = Base`) stays self-consistent. All three round-4 FP repros stay silent; the wave-15 `a_carrier_declaring_only_another_metafield_is_silent` fixture is kept unchanged and the discriminating fixture Shockwave said was missing (same carrier + one colon method) is added beside it. Also closes the reopened **infer.rs:888** thread: that comment points at the canonical Counter shape as the rule's job, and the rule covers it again. **P** — the LB0510 subsection asserted two things the code does not do. "whose `__index` is written ... through a local alias" is now TRUE (a write is required) and is kept, made precise. "it is an operator metatable, not a broken class" asserted intent the rule cannot know; rewritten to describe the actual check ("a carrier that declares another metafield AND has no instance methods"). `__gc`/`__name` disclosed; the in-file suppressions enumerated rather than run together in one sentence. **M** — the residual-arm comment claimed the gate "deliberately runs edition legality only", untrue since round 4. Rewritten around Shockwave's own verification: the arm's primary service is a finding lowering *introduces* — `<close>` at target 5.3 wraps the scope tail in a pcall'd function, putting a function boundary between a `goto` and its label. Re-measured here: `luabox check` exit 0, `luabox build` exit 1 with `error[LB0020]` + "the lowered output of `src/main.lua` cannot load on target 5.3", `dist/` absent. Spanless is honest there. The three gate-blind paths are then named, each verified with the release binary: (1) a module under `lua_modules/` — check reports "0 errors in 1 files", build refuses naming `lua_modules/share/lua/5.1/dep.lua`, no artifact (this lands in the *bundle-path* twin, and the comment says so); (2) `--out` at a source directory — check reports "0 errors in **0** files", the bundler still refuses; (3) LB0014/15/16 in tree mode — check exit 0, build exit 1 with "has no lowering rule for target 5.1", `dist/` absent. One sentence acknowledges that (1) and (2) are two faces of one design question (check and build disagreeing about the project's file set) and that aligning them is the owner's call — **deliberately not taken this round**, per the brief. **R** — `--target` said "Also validate dialect legality against a ship target", wrong twice; now "Validate against a ship target on both axes — dialect legality and loader (control-flow) legality; `[build] target` already drives the loader half, and this overrides it", at sibling length. SPEC §5 gains a bullet next to `target = "5.1"` stating which passes it drives per command. **S** — module doc now names the ship target's control-flow pass in the gate description (agreeing with `check_gate`'s own doc at :226-238) and names `luabox_bundle::load_module` as the twin the three bundle-shaped emits go through. **N** — the startup harvest had no token: Shockwave captured 746 ms of protocol silence between the `initialize` response (2.1 ms) and the bootstrap token (748.2 ms), covering a 0.1 ms index. The obstacle was structural, not protocol: the harvest ran before the `Server` existed. `Server::new` now builds the struct first (rocks default-empty) and runs the harvest through `harvest_announced`, the same helper the reload uses. **Protocol legality checked, not assumed**: LSP bars the server from speaking only *until it has responded to `initialize`*, and `initialize_finish` has already run — the bootstrap token has been sent from that same window since it shipped, which is the existing precedent. Distinct token ids (`luabox/rock-harvest`, `luabox/reload`) so a client can tell the pauses apart; the integration helper is now token-aware because a helper that stopped at the first `end` would have reported the wrong token. **O** — `window.workDoneProgress` was read into a local and passed to `bootstrap` alone, so the reload announced itself to every client. The flag now lives on `Server` and gates `begin_progress_titled` (`Option<ProgressToken>`). Both directions tested, in-process and over the real transport: a client without the capability receives **zero** `$/progress` on all three paths; one with it hears all three. **Q** — `scripts/lsp-startup-bench.sh:17` still quoted the retracted 3.84x, in the file whose purpose is stopping drift; it now quotes no ratio and points at `harvest_rock_tree`'s table. That table records its host — `Intel(R) Xeon(R) Processor @ 2.80GHz`, 4 vCPU, containerized sandbox, 7 runs — and marks the sequential row single-host and non-reproducing, with Shockwave's max (2384 ms) below this host's min (2673 ms) stated as the evidence. CHANGELOG's copy carries the same qualification. **pin_worker_stacks** — **DEVIATION, flagged with evidence**: a "delete the pin and watch a worker overflow" test cannot be constructed, and the reason is now at the call site rather than left as a gap. Recursion is capped by the parser's `MAX_DEPTH`; `luabox-syntax`'s `parsing_at_the_depth_limit_fits_a_default_stack` already proves 2 MiB holds at that cap in a *debug* build, and `a_deep_rock_tree_survives_the_startup_harvest` makes the same point for the whole pipeline through deliberately *unpinned* workers. Probed empirically: raising that test's depth from 195 to `MAX_DEPTH` (220) does not overflow — the parser bails at the limit and the files simply stop harvesting. So no input overflows an unpinned worker while fitting a pinned one. What *is* testable is now tested: the pin is idempotent and never fatal (the `Err` arm production actually takes on the CLI path), and `PINNED_STACK_BYTES` is asserted equal across `luabox-lsp` and `luabox-cli`, so "mirroring the CLI's" stops being a comment two crates can drift on. **Non-blocking bundle, all four done**: *K* — preflight grep widened from `error[LB0001]` to `error[LB000[0-9]]` (unallocated headroom in the parse band), plus a header paragraph stating the invariant is enforced on the parse/dialect axis only and why the control-flow axis needs no check today (5.2 is the loosest control-flow dialect modelled, so the column edition cannot reject what a target accepts — unreachable rather than unasserted). *E residual* — passes 2 and 3 were each sorted then concatenated in pass order. Measured on a 5.2 project with `::a:: ::a::` at line 1 and `1 // 2` at line 2: BEFORE `LB0011` (2:13) then `LB0021` (1:9); AFTER `LB0021` (1:9) then `LB0011` (2:13). Both axes now sort together, stably, so a tie keeps dialect-legality ahead of loader-legality. No scenario pinned the old order; one now pins the new one. *server.rs code-action source* — the pairing site hardcoded `LINT_SOURCE`; both publishers now go through one `diagnostics::source_for(code)` keyed on `Code::is_lint`, pinned on the band boundary rather than on whichever codes carry fixes today (a synthetic suggestion-carrying non-5xx was not cheap, so the helper + boundary test is the cover). *Scenarios* — `mode = "nvim-plugin"` control-flow refusal (the third bundle-shaped emit, the only one with no scenario); tree-mode build against a manifest target with no flag; flag-overrides-manifest on the build path; the LB0014/15/16 check-silent/build-fatal rows as an outline; and the vendored-dependency spanless-but-refusing behaviour, pinned. **Gates** (exit codes checked directly, never through pipes): `cargo fmt --all --check` 0; `cargo clippy --workspace --all-targets -- -D warnings` 0; `cargo test --workspace` **2404 passed / 0 failed**; acceptance **860 scenarios / 4678 steps**; lsp_acceptance **194 scenarios / 1406 steps**; `control-flow-differential.sh` **112/112 cells agree** (5.2/5.3 SKIP — no `luac5.2`/`luac5.3` on this box). **Coverage**: unit aggregate **95.55%** (floor 95); per-crate all >= 92 — bundle 95.92, cli 93.03, db 99.46, diag 97.09, hir 98.47, lint 99.11, lower 97.93, lsp 97.40, manifest 96.86, syntax 97.57, types 96.90; e2e **85.21%** (floor 83). CHANGELOG has an Unreleased *Fixed* entry for the LB0510 regression and *Internal* entries for the progress gating, the three stale claims, and the diagnostic ordering. **Could not verify locally**: `luac5.2`/`luac5.3` are absent, so four differential columns SKIP here and only CI exercises them; the LSP bench was not re-run under the new token (the committed harness times to first `publishDiagnostics`, which the token does not move) so the 746 ms window being inside a token is argued from the code path and the unit/integration progress tests rather than from a fresh protocol capture.
