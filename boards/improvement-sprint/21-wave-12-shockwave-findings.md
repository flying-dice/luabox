---
column: doing
labels: [wave, review-round]
priority: high
agent: opus-w12
live: true
status: all six fixed, pushed
updatedAt: 2026-07-29T17:53:00.000Z
---
# Wave 12 — Shockwave's 6 PR findings

1) Atomic writes for fmt/lint --fix (temp+fsync+rename; symlink-resolved). 2) EPIPE exits with the recorded verdict, not 0. 3) Peak-RSS leg in perf-gate.sh. 4) Draft-install: wget fallback, jq fail-fast, mock-server exercise in ci.yml via a base-URL override. 5) Depth headroom test through lower+check+fmt+bundle. 6) partition_batches/next_batch parity bridge. PR #45 review thread is the spec.

## Comments

- **opus-w12** (2026-07-29T17:53:00.000Z): All six fixed on `sprint/w12-shockwave-findings`. **F1** atomic replace: new `crates/luabox-cli/src/atomic_write.rs` (`write_atomic` — canonicalize, sibling `.luabox-<pid>-<n>.tmp`, fsync, copy mode, rename; temp name invisible to `layout::is_in_project_tree`/`is_project_source` and `watch::is_relevant` by three independent rules), wired at `crates/luabox-cli/src/fmt_cmd.rs:64` and `crates/luabox-cli/src/lint_cmd.rs:145`; audited every other write site — scaffold creates, build/doc/modes write luabox-owned output, left alone. Reviewer's repro reproduced exactly (147,818 B source → 8,192 B before; byte-identical after, sha256 unchanged) and regression-pinned in `crates/luabox-cli/tests/atomic_rewrite.rs` (4 tests, verified failing against the pre-fix code). ENOSPC leg gives the clean error: `Error: cannot write \`src/main.lua\`` + exit 1, temp removed. **F2** `crates/luabox-cli/src/emit.rs:59-99,148` `set_exit_on_reader_gone`/AtomicI32, recorded in `crates/luabox-cli/src/project.rs:47` (covers check/lint/doc/build — all five callers fail iff errors>0) and `crates/luabox-cli/src/fmt_cmd.rs:78` for `fmt --check`'s non-diagnostic list; module docs rewritten off the ripgrep convention. `set -o pipefail; check | head -1` on a 6000-error tree: 0 → **1**; clean tree still 0; `tests/broken_pipe.rs` 4 → 11 tests. **F3** `scripts/perf-gate.sh:158-193` peak-RSS leg via new `scripts/peak-rss.py` (wait4 rusage, python3 stdlib, SKIPs loudly without it); measured **123 MiB** (3 identical runs) vs budget **300 MiB**, `LUABOX_RSS_BUDGET_MIB`, not scaled by `LUABOX_PERF_FACTOR`; mirrored in `scripts/perf-gate.ps1:196-232` (PeakWorkingSet64, unexecutable here); `decisions/07` + `.github/workflows/ci.yml:114-137` updated (7 → 8 legs). Full gate green. **F4** `scripts/install.sh`: curl-or-wget layer (`http_client`/`http_fetch`/`http_body`, bash-3.2-safe empty-array expansion), `fetch_asset_by_id` hand-rolls the redirect under wget because wget forwards `--header` across hosts, jq check moved to the `LUABOX_DRAFT_INSTALL=1` guard (fail-fast), new `LUABOX_API_BASE`; `scripts/tests/mock-release-api.py` + `scripts/tests/draft-install-mock.sh` (tag on page 2, storage 400s on a forwarded token, real tar.gz+SHA256SUMS) run as the new `draft-install-mock` job in ci.yml; all 4 legs pass locally and the wget auth-drop was mutation-verified. `release.yml` verify gains `jq --version`; `install.ps1` mirrors `$ApiBase`; `docs/02-guides/01-releasing.md` says what is now mock-covered and what only a real tag still tests. **F5** `crates/luabox-cli/tests/deep_pipeline.rs` drives the real binary (check, check --target, fmt, fmt --check, build --bundle --minify --sourcemap) over 5 constructs at depth 195; workspace test, no platform gate, so ci.yml's check matrix runs it on all three OSes — that is what closes the Windows 1 MiB main-stack gap. **F6** `crates/luabox-cli/src/watch.rs`: `next_batch` takes the window, `BATCHING_CASES` is one table both the model and the live loop consume, `the_model_and_the_live_loop_batch_every_case_identically` asserts parity (mutation-verified: a sliding-window model fails it); module doc's "the two agreeing is the point" is now a named assertion. Verification: `cargo test --workspace` **2236 passed / 0 failed** (from 2215) + 740 acceptance + 187 lsp scenarios; clippy `-D warnings` and `cargo fmt --all --check` clean; unit coverage **96.67%** aggregate (floor 95), luabox-cli **92.92%** (floor 92, all 11 crates pass); e2e **86.00%** (floor 83). Known limits stated in the report: under `ulimit -f` the process still dies on SIGXFSZ (exit 153) so a staged `.tmp` can be orphaned — inert and invisible; hardlinks break by design; perf-gate.ps1 and install.ps1 are review-verified only (no pwsh).
