---
column: doing
labels: [release, review]
priority: high
agent: opus-w15
live: true
status: All 15 threads replied+resolved; awaiting Shockwave round 5
updatedAt: 2026-08-01T05:10:00.000Z
---
# Wave 15: Shockwave round-4 threads on PR #47

Round 4 @ 70a6b3a: A/C/5 CLOSED on measurement; 1/3/4/B PARTIAL (reopened); new D/E/F/G (bugs), H/I/J/K/L/M (issues). Verdict FAIL, 15 open threads (Shockwave's corrected count — finding I is two threads). refs #47

Priority by blast radius (Shockwave's ranking): D (bundle modes ship unloadable artifacts, exit 0 — release-blocking), F/G (LB0510 cries wolf on operator-only metatables + misses cross-file shape), E (--target dedup mis-attributes first-defined-here, out-of-order output), I (LSP perf number unfalsifiable; stop-short premise contradicted by 946ms config-reload stall).

## Scope

- D: control_flow residual in luabox-bundle (~6 lines at lib.rs:357, HIR in hand) + bundle-mode acceptance scenarios
- E: dedup key includes secondary labels / prefer target verdict on disagreement; sort by span before render
- F: LB0510 silent on metafield-only carriers and aliased carriers
- G: LIMITATIONS states in-file-only + lint-only; fix rawset literal-key over-suppression
- H: [build] target feeds check passes when no CLI flag
- I: committed harness under scripts/, reconciled numbers (654 vs 545 contradiction), corrected premise comment (reload_config path), rayon pool pinned/documented for library entry points
- J: carrier tie-break last-wins (Lua binding semantics) not first-wins
- K: differential target column edition assumption enforced
- L: CHANGELOG partial-emit wording corrected
- M: build residual pass prefers spanned source-level findings

## Comments

- **claude** (2026-08-01T02:40:00.000Z): Worktree /home/user/wt-w15, branch sprint/w15-shockwave-round4 off develop@70a6b3a. Opus agent launched with full repro set from the round-4 review + inline threads.
- **opus-w15** (2026-08-01T03:40:00.000Z): All 13 round-4 threads fixed on `sprint/w15-shockwave-round4` (5 commits, a81f3a7..HEAD). **D** — the residual control-flow pass lived on the tree-mode path only; `luabox_bundle::bundle` ran parse errors + `lua::validate::validate` and never `validate::control_flow`, so all three bundle shapes shipped the illegal chunk. Fixed at crates/luabox-bundle/src/lib.rs:359-382 where the HIR is already in hand, gated on a clean parse by the early return above it. Measured with the release binary, edition 5.2 / `--target 5.4` / src `::a:: do ::a:: end`: BEFORE `bundle = true` exit 0 with `dist/main.lua` containing the illegal chunk; AFTER exit 1, `dist/` absent. Same for `mode = "love"`. Pinned at the library boundary (two `luabox-bundle` unit tests, both directions) AND as acceptance scenarios, because the check gate now catches the CLI case earlier and would otherwise mask a bundler regression. **H** — `run_once` took the target from the CLI flag only. Fixed at crates/luabox-cli/src/check_cmd.rs:99-160. BEFORE: edition 5.2 + `[build] target = "5.4"` + `luabox check` exit 0 while `check --target 5.4` exit 1; AFTER both exit 1 with the same LB0021. **DEVIATION, flagged**: the brief said fall back to the manifest target for "the dialect passes" (both). I applied it to the control-flow pass only. Measured reason: with both, a 5.3 project declaring `target = "5.1"` and using `local x = 7 // 2` failed `luabox check` with LB0011 — a construct lowering rewrites, and `luabox build` emits `math.floor(7 / 2)` happily. Failing `check` for using the feature `[build] target` exists to provide is a worse bug than the one being fixed. The split is now explicit (`TargetPasses`, check_cmd.rs:117-160): dialect legality asks "would this source parse there?", control flow asks "would its loader accept it?"; `--target` asks both, `[build] target` and `build`'s check gate ask only the second. Four scenarios pin both directions. **E** — pass-3 dedup keyed (code, primary span) and kept whichever pass ran first. Repro measured BEFORE: `4:5 first defined here 3:5` then `3:5 first defined here 1:3` — 3:5 reported as a duplicate AND cited as the first-definition site, out of source order. AFTER: `3:5 first defined here 1:3` then `4:5 first defined here 1:3`, i.e. exactly an edition-5.4 run, in source order. Both passes now merge through one `Findings` helper (check_cmd.rs:479-521): later pass wins a key both fill (the ship target is the verdict that decides whether the artifact loads), target-only findings say so — pass 2 in the label (`not legal on target 5.1`, which used to read `not legal in this edition` counterfactually), pass 3 in a note — and the merged set is sorted by span before rendering. **M** — falls out of H: `build`'s LB0021 now comes from the source-level gate, so it carries `duplicate label` + `first defined here` + `src/main.lua:1:12` instead of the spanless reconstruction. **DEVIATION, flagged**: the brief asked for a source-level pass inside `lower_one` and the new bundle pass with code-dedup against the residual. With the gate carrying the effective target, that pass would be unreachable from every CLI path — dead code — so the source-level judgement lives in the gate (one place, spanned) and the residual passes stay exactly as belt-and-braces for findings lowering itself introduces. No double reporting by construction: the gate bails before any emitter runs. **F** — two false-positive classes closed in crates/luabox-lint/src/rules/metatable_without_index.rs. Operator-only metatables (`function Vec.__tostring(v)`, and `__call`/`__mode`/`__add`/`__eq`) now suppress via a second `operator_tables` set — Shockwave's cheap discriminator, "any other metafield". Aliased `__index` writes (`local mt = Counter; mt.__index = mt`) suppress by treating a local alias of the carrier as settling, the same conservative trade as a computed key. The note stopped being counterfactual: `every t:method() is ... at runtime` -> `so any t:method() fails at runtime with ...`. True positives re-pinned in the same block (a carrier with only plain fields still fires). **G** — (a) the `rawset` arm settled on ANY `rawset(C, ...)` though `index_key` already reads literal keys; now only a literal `__index` or an unreadable key settles, a literal metafield reads as an operator table, `rawset(C, "n", 0)` fires again. Key classification is one `KeyKind` enum shared by the assign, table-constructor and rawset arms. (b) docs/03-reference/02-limitations.md gained a "What that lint does *not* cover" subsection stating plainly that LB0510 is IN-FILE ONLY (with the cross-file `require`d-carrier program spelled out as a known silent runtime crash) and LINT-ONLY (never affects `check`'s exit code), plus the never-called-function / dead-branch / alias / computed-key suppressions. Cross-file analysis deliberately NOT attempted; `check` deliberately NOT made to surface it. **I** — five parts. (1) `scripts/lsp-startup-bench.sh` + `scripts/lsp-startup-bench.py` + `gen-corpus --rock-tree [X.Y]` are committed: the flag writes the generated modules into `<out>/lua_modules/share/lua/<X.Y>/` with a `luabox.toml` and `src/main.lua`, and the script builds, generates, runs N timed `initialize`->first-`publishDiagnostics` cycles over the real stdio protocol and prints median/min/max. `--threads N` pins `RAYON_NUM_THREADS` so the sequential baseline is the same binary. Not a CI gate, by design. (2) RE-MEASURED here, 4 vCPU, 50 files / 102,813 lines, 7 runs each: parallel median **759.8 ms** (min 717.6, max 868.1); `RAYON_NUM_THREADS=1` median **2750.5 ms** (min 2673.0, max 3204.2) — **3.62x**. Same project with the tree removed: 10.5-15.1 ms over 5 runs, so the harvest is effectively the whole wait. Our old figures (2270 -> 545, 3.8x) do not reproduce on this box; the comment and CHANGELOG now carry the re-measured pair and say the ratio is host-dependent. (3) CONTRADICTION GONE: rocks.rs:234's "2270 ms -> 654 ms" is replaced by a pointer to `harvest_rock_tree` as the single source — a second copy of a measurement is how they diverged. (4) PREMISE FIXED: the comment claimed the wait happens "once, at startup"; `reload_config` re-harvests on the main loop from `didChangeConfiguration` and from watched `luabox.toml` edits. Both call sites are now named, and the reload harvest is wrapped in a work-done progress token (`begin_progress_titled`/`end_progress`, factored out of the bootstrap token) so the stall is visible in-editor — a label on the pause, not a restructuring of the loop; pinned by a test. The synchronous decision stands with the reload named as the honest reason to reopen it. (5) POOL PINNED for library callers: `run_stdio` and `run` both call a best-effort `ThreadPoolBuilder::stack_size(16 MiB).build_global()`. `test_server` deliberately does NOT pin, and `a_deep_rock_tree_survives_the_startup_harvest` drives 32 rock modules at depth 195 through it, so the unpinned 2 MiB-default path is what the test exercises. **J** — crates/luabox-types/src/env.rs:1683 `entry().or_insert_with()` -> `insert()`: within the lexical rank the LAST carrier wins a repeated variable name, which is the binding `function M:m()` names. Lexical-over-nominal is a different axis and is untouched (the wave-14 both-orders tests pass unmodified). The old pinning test is inverted and renamed `the_last_carrier_wins_a_repeated_variable_name`; three shapes added as unit tests and three defs-vs-project parity scenarios in defs.feature. **K** — `scripts/tests/control-flow-differential.sh` now enforces the target-column invariant in a preflight: every matrix program is checked once at `TARGET_COLUMN_EDITION` with no `--target`, and any LB0001 or LB0010-LB0019 fails the script loudly, naming the program and explaining that exceptions.tsv (keyed program+version) cannot express a column dimension. **DEVIATION, minor**: the addendum said "any reject", but half the matrix is deliberately illegal programs that `check` rejects at 5.2 with LB0020-LB0022 — a blanket reject test fails on the first `illegal_*` file. Keyed on parse/dialect codes instead, which is what the original brief said and what the invariant actually is. Proven by dropping `local x = 7 // 2` into the matrix: exit 1 naming `zz_tmp_violation`. **L** — CHANGELOG:29's "refuses to write anything when it fails" replaced with the per-file guarantee (nonzero exit, failing file's output absent, cleanly-lowered files still emitted) plus the note that `build` never cleans `dist/`, so a failing rebuild leaves the previous run's output in place. **Non-blocking items, all done**: validate.rs's 5.1 `goto` guard is now load-bearing in test (a file parsed+lowered under 5.2, asserted to contain a `Stmt::Goto`, judged at 5.1 — verified by deleting the guard and watching the test fail); a server test asserts the returned `CodeAction.diagnostics` is byte-identical to what `publish_lua` emitted, over a mixed set (LB0022 + LB0501); layout.rs:376 carries the comment explaining why the defs/ walk deliberately orders the opposite way to its sibling rock walk; the `..._path_sorted` test is renamed to `..._in_require_candidate_order`. **Gates** (exit codes checked directly): `cargo fmt --all --check` 0; `cargo clippy --workspace --all-targets -- -D warnings` 0; `cargo test --workspace` 2383 passed / 0 failed; acceptance 852 scenarios / 4613 steps; lsp_acceptance 194 scenarios / 1406 steps; control-flow differential 112/112 cells agree (5.2/5.3 SKIP, no luac on this box). **Coverage**: unit aggregate 95.55% (floor 95); per-crate all >= 92 — bundle 95.92, cli 93.02, db 99.46, diag 97.09, hir 98.47, lint 99.07, lower 97.93, lsp 97.38, manifest 96.86, syntax 97.57, types 96.90; e2e 85.24% (floor 83). CHANGELOG has Unreleased *Fixed* entries for D, H, E, M and F and *Internal* entries for I, J, K and G's documentation. **Could not verify locally**: `luac5.2`/`luac5.3` are absent, so those four differential columns SKIP here and only CI exercises them; the pre-fix sequential LSP build no longer exists, so the parallel/sequential comparison uses the same binary at `RAYON_NUM_THREADS=1` rather than reproducing the historical 2270 ms row.
- **claude** (2026-08-01T04:55:00.000Z): Wave 15 merged to develop (add/add conflict on this card resolved keeping both the scope section and the journal). Spot-checks on the merged head, then thread replies on PR #47.
- **claude** (2026-08-01T05:10:00.000Z): Merged head 29e97b1 pushed. Spot-checks all green: clippy 0, fmt 0, acceptance 852/4613, lsp_acceptance 194/1406, differential 112/112 (5.2/5.3 SKIP locally). All 15 round-4 threads replied (thread -> fix commit with before/after measurements, deviations flagged: H control-flow-only manifest fallback, M gate-not-residual, K parse/dialect-code preflight) and resolved. PR body rewritten: rounds 2-4 section, head-measured numbers (2383 tests, 852/4613, 95.55/93.02/85.24). Awaiting Shockwave round 5 (triggers on push).
