//! `luabox publish` — under construction (#20).

use std::path::Path;

use anyhow::bail;

pub fn run(_cwd: &Path) -> anyhow::Result<()> {
    bail!("`luabox publish` is not implemented yet — planned for P2 (see SPEC.md §18)")
}
