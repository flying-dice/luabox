# Reviewed surviving mutants (#58). scripts/tests/mutants-gate.sh diffs
# the live missed set against column 1 of this file (tab-separated: the
# cargo-mutants missed line as first seen, then the reviewed reason). A
# survivor not listed here fails the job; a listed line that stops surviving
# is reported as stale to prune — unless EVERY line goes stale in one run,
# which is a scope that moved rather than an allowlist that was earned, and
# fails.
#
# TIMED-OUT mutants are judged against this same file: a timeout is an
# unkilled mutant, not a killed one, so a new one fails the job and a listed
# line that flaps missed -> timeout stays reviewed (and is not stale).
# The line format is identical either way, so no separate section is needed.
#
# LINE NUMBERS ARE INFORMATIONAL. The gate keys the comparison on (file,
# mutant text) and pairs live mutants against these lines as a multiset —
# same position first, then the leftovers — so an edit above a waived mutant
# is REPORTED as a measured shift (old and new position both printed) and a
# mutant with no (file, text) match at all is what fails the job. Re-pin the
# printed positions when convenient; the judgement is the gate's, not the
# reader's. Keying on the whole line put that judgement here instead, and
# the rule's first exercise was a mixed batch — four shifts and one
# genuinely new mutant (2026-08-05) — which the counts alone cannot separate.
#
# Reason classes (reviewed 2026-08-02; run of 407 mutants over the
# merge-seam scope: 282 caught / 39 missed -> 13 killed by new tests,
# now 16 waived below, every count MEASURED by a full verification run:
# 11 of the original 26 [gap] lines got kills in the sprint extension
# (enum-member dotted reads, collision intra-file dedup, the
# assign_target_name name variants, version on assigned signatures, the
# NAME = {} classifier, own-declaration conformance exclusion, the
# truncated-long-bracket guard). Three claimed kills did NOT hold — the
# attach_typed_functions no-op pair and the dotted-target arm survive
# because the local-table shape path independently checks those calls —
# and are re-waived below with what the measurement showed):
#   [equivalent]  semantically identical / reduces to a no-op — unkillable
#   [defensive]   guards malformed empty-name harvest output that the
#                 public checker surface cannot name or observe
#   [label]       affects only a secondary label/span; suites assert codes
#   [gap]         real crate-local coverage gap, acknowledged — exercised
#                 via the cli e2e suites or narrower than a crate test
#                 reaches today; candidates for future kills
#   [hang]        the mutation makes a loop non-terminating, so the only
#                 observable effect is the suite never finishing — the run
#                 timeout IS the detection; no assertion can complete to
#                 kill it (surfaced by the gate's timeout rule, which the
#                 old gate silently dropped)
#
crates/luabox-types/src/env.rs:132:38: replace match guard !c.name.is_empty() with true in FileTypes::collect	[defensive] empty-name tag guard — an unnameable declaration cannot be observed through the checker
crates/luabox-types/src/env.rs:148:37: replace match guard !e.name.is_empty() with true in FileTypes::collect	[defensive] empty-name tag guard — an unnameable declaration cannot be observed through the checker
crates/luabox-types/src/env.rs:153:38: replace match guard !a.name.is_empty() with true in FileTypes::collect	[defensive] empty-name tag guard — an unnameable declaration cannot be observed through the checker
crates/luabox-types/src/env.rs:717:34: replace match guard !c.name.is_empty() with true in TypeEnv::absorb_block	[defensive] empty-name tag guard — an unnameable declaration cannot be observed through the checker
crates/luabox-types/src/env.rs:858:37: replace match guard !o.op.is_empty() with true in TypeEnv::absorb_block	[defensive] empty-name tag guard — an unnameable declaration cannot be observed through the checker
crates/luabox-types/src/env.rs:879:33: replace match guard !e.name.is_empty() with true in TypeEnv::absorb_block	[defensive] empty-name tag guard — an unnameable declaration cannot be observed through the checker
crates/luabox-types/src/env.rs:888:33: replace match guard !c.var.is_empty() with true in TypeEnv::absorb_block	[defensive] empty-name tag guard — an unnameable declaration cannot be observed through the checker
crates/luabox-types/src/env.rs:1950:34: replace match guard !c.name.is_empty() with true in carrier_var_classes	[defensive] empty-name tag guard — an unnameable declaration cannot be observed through the checker
crates/luabox-types/src/env.rs:298:30: replace > with >= in TypeEnv::build_from_items	[defensive] whitespace-walk bound on the inline-@as anchor; anchor==0 needs a cast with no expression before it, which the grammar does not produce
crates/luabox-types/src/env.rs:301:27: replace > with >= in TypeEnv::build_from_items	[defensive] whitespace-walk bound on the inline-@as anchor; anchor==0 needs a cast with no expression before it, which the grammar does not produce
crates/luabox-types/src/env.rs:1288:9: replace TypeEnv::typed_local_span -> Option<std::ops::Range<usize>> with None	[label] declared-here span for the duplicate-field note; suites assert codes, not label spans
crates/luabox-types/src/env.rs:1333:9: replace TypeEnv::is_class -> bool with true	[equivalent] measured, not assumed: two candidate separating shapes (alias-typed local from a literal, alias-typed parameter) behave identically under the mutation — alias reads take the shape/alias arms, so the Named-absence arm this gates appears to see only real class names in practice; the alias leniency itself is now pinned in undefined_field.rs either way
crates/luabox-types/src/env.rs:1798:22: replace && with || in class_param_unification	[equivalent] with || the guard admits only cases whose rename map is the identity (own==canonical) or empty (own empty) — a no-op substitution either way
crates/luabox-types/src/env.rs:982:9: replace TypeEnv::attach_typed_functions with ()	[gap] measured twice: the dotted call-site probe passes in BOTH worlds — the local-table shape path independently checks these calls, so the attachment's only unshared consumers are cross-crate dotted-name lookups with no shape in scope; a defs-path probe is the remaining kill candidate
crates/luabox-types/src/env.rs:996:16: delete ! in TypeEnv::attach_typed_functions	[gap] measured twice: the dotted call-site probe passes in BOTH worlds — the local-table shape path independently checks these calls, so the attachment's only unshared consumers are cross-crate dotted-name lookups with no shape in scope; a defs-path probe is the remaining kill candidate
crates/luabox-types/src/env.rs:1548:9: delete match arm Expr::Field(field) in assign_target_name	[gap] measured twice: the dotted call-site probe passes in BOTH worlds — the local-table shape path independently checks these calls, so the attachment's only unshared consumers are cross-crate dotted-name lookups with no shape in scope; a defs-path probe is the remaining kill candidate
crates/luabox-types/src/env.rs:299:28: replace -= with /= in TypeEnv::build_from_items	[hang] the inline-@as anchor walk: `anchor /= 1` leaves anchor unchanged, so the whitespace-skip while-loop never terminates when the preceding byte is whitespace — detected only as a hang; the run timeout is the kill
