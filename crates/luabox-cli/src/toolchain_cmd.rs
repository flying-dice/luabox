//! `luabox toolchain` — under construction (#27).

use anyhow::bail;

pub fn install(_version: &str) -> anyhow::Result<()> {
    bail!("`luabox toolchain install` is not implemented yet — planned for P4 (see SPEC.md §18)")
}

pub fn pin(_version: &str) -> anyhow::Result<()> {
    bail!("`luabox toolchain pin` is not implemented yet — planned for P4 (see SPEC.md §18)")
}

pub fn list() -> anyhow::Result<()> {
    bail!("`luabox toolchain list` is not implemented yet — planned for P4 (see SPEC.md §18)")
}
