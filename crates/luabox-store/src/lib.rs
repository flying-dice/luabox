//! Content-addressed store — **Distribution** bounded context (SPEC.md §6, §16).
//!
//! Global CAS cache (`~/.luabox/store`), hard-link/reflink into projects
//! (bun/pnpm model), artifact fetch and hash verification.
//!
//! Status: placeholder — P2.
